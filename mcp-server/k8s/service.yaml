apiVersion: v1
kind: Service
metadata:
  name: debugtools-mcp-service
  namespace: default
  labels:
    app: debugtools-mcp-server
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: debugtools-mcp-server
